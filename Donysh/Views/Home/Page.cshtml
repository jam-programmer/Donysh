@model PageDetail
@{
    ViewData["Title"] = $"{Model.Title}";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@await Component.InvokeAsync("HeaderComponent",$"{Model.Title}");

<div class="container">
    <div class="row">
        @Html.Raw(Model.Body)
    </div>
</div>